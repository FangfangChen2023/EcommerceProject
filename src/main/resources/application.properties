spring.datasource.url = jdbc:mysql://localhost:3306/ecommerce_project
spring.datasource.username=root
spring.datasource.password=123456

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create

# ddl-auto:
# create => drop the previous table then create a new table every time (do not use in production)
# update => if the table does not exist then create it. If the table exists, update it.

# Springboot will internally create the DataSource bean for us